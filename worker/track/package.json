{
  "name": "lp-tracking-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-tenant tagging server (Meta CAPI + GA4 MP + Google Ads) for *.b2tech.io landing pages. ADR 0021 / SPEC-015.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute lp-tracking --file=./schema.sql --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.0.0"
  }
}
